server:
  port: 8080 # Boot stays on 8080
  shutdown: graceful
  tomcat:
    connection-timeout: 20000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nexus_connect
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  lifecycle:
    timeout-per-shutdown-phase: 30s
  sql:
    init:
      mode: never

jwt:
  secret: ${JWT_SECRET:netwrok-programming-nexus-connect}
  expiration: ${JWT_EXPIRATION_MS:3600000}

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
      - http://localhost:4173
      - http://127.0.0.1:4173
      - http://192.168.*.*:*
      - http://10.*.*.*:*
      - http://172.*.*.*:*
      - https://0c7vjbtk-5173.asse.devtunnels.ms

nexus:
  nio:
    port: 8081
